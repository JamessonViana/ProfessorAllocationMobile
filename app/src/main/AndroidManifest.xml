<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/net_seguranca"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProfessorAllocation"
        tools:targetApi="31">
        <activity
            android:name=".Main.UI.AllocationActivity"
            android:exported="false" />
        <activity
            android:name=".Main.UI.DepartmentActivity"
            android:exported="false" />
        <activity
            android:name=".Main.UI.ProfessorActivity"
            android:exported="false" />
        <activity
            android:name=".Main.UI.CourseActivity"
            android:exported="false"
            android:theme="@style/Theme.ProfessorAllocation" />
        <activity
            android:name=".Main.UI.CourseActicity"
            android:exported="false"
            android:label="@string/title_activity_course_acticity" />
        <activity
            android:name=".Main.UI.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>